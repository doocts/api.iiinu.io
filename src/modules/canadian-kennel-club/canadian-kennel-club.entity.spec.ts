import { CanadianKennelClubEntity } from './canadian-kennel-club.entity';

describe('CanadianKennelClub', () => {
  it('should be defined', () => {
    expect(new CanadianKennelClubEntity()).toBeDefined();
  });
});
